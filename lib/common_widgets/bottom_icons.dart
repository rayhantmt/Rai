import 'package:flutter/material.dart';

class BottomIcons extends StatelessWidget {
  final String tittle;
  final String img;
  const BottomIcons({super.key, required this.tittle, required this.img});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [

      ],
    );
  }
}