import 'package:flutter/material.dart';

class MoreSectionWidget extends StatelessWidget {
  const MoreSectionWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Row(
      children: [
        
      ],
    );
  }
}