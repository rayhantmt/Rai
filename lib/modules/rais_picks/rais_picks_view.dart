import 'package:flutter/material.dart';

class RaisPicksView extends StatelessWidget {
  const RaisPicksView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Raispicks'),
      ),
    );
  }
}